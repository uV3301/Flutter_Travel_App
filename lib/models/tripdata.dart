class tripdata {
  String title;
  DateTime startdate;
  DateTime enddate;
  double budget;
  String traveltype;
  tripdata(
      this.title, this.startdate, this.enddate, this.budget, this.traveltype);
}
